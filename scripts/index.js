// CSS-only parallax using background-attachment: fixed
// No JavaScript needed for basic parallax effect

document.addEventListener("DOMContentLoaded", () => {
  console.log("DOM loaded - Using CSS-only parallax with background-attachment: fixed");
  console.log("Hero sections found:", document.querySelectorAll(".hero-section").length);
});
